# Kern (immer benötigt)
pydantic>=2.6
pyyaml>=6.0

# API
fastapi>=0.110
uvicorn[standard]>=0.29

# Referenz-Backend (Model Core, Training)
torch>=2.2

# Optional, für spätere Backends/Hardware -- bewusst NICHT in
# requirements.txt fest verdrahtet, sondern in requirements-optional.txt,
# damit die Engine ohne diese Extras startfähig bleibt:
#   jax, jaxlib          (JAX-Backend)
#   torch-xla            (TPU über PyTorch)
